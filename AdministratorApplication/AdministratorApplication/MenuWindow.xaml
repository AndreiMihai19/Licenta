<Window x:Class="AdministratorApplication.MenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdministratorApplication"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" 
        Title="Menu
        " Height="700" Width="1300"
        Icon="./Resources/icon1.ico" 
        FontWeight="Bold">

    <Window.Resources>
        <SolidColorBrush x:Key="ButtonColor" Color="#317773"/>
        <SolidColorBrush x:Key="ButtontTextColor" Color="White"/>
        <SolidColorBrush x:Key="ButtonHoverColor" Color="#193b39"/>

        <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{DynamicResource ButtonColor}"/>
            <Setter Property="BorderBrush" Value="#676b6b"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtontTextColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="btnBorder" CornerRadius="15" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="ContentPresenter" Focusable="False" 
                                               HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                               VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                               SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                               RecognizesAccessKey="True">
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{DynamicResource ButtonHoverColor}"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel Orientation="Horizontal" Height="50" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10">
            <Button x:Name="BtnEmployees" Content="Angajati" Style="{DynamicResource ButtonStyle}" VerticalContentAlignment="Center" Click="BtnEmployees_Click" FontSize="20" IsEnabled="True"/>
            <Button x:Name="BtnEmployeeAdd" Content="Adaugare angajat" Style="{DynamicResource ButtonStyle}"  Margin="10,0,0,0" Click="BtnEmployeeAdd_Click" FontSize="20"/>
            <Button x:Name="BtnStatus" Content="Status" Style="{DynamicResource ButtonStyle}" VerticalContentAlignment="Center" Margin="10,0,0,0" FontSize="20" Click="BtnStatus_Click"/>
            <Button x:Name="BtnExit" Content="Iesire" Style="{DynamicResource ButtonStyle}" Margin="10,0,0,0" FontSize="20" Click="BtnExit_Click"/>
        </StackPanel>
        <Frame x:Name="Main" Margin="0,50,0,0" NavigationUIVisibility="Hidden"/>
        <ProgressBar  x:Name="PageProgressBar" Background="red" IsIndeterminate="True" Visibility="Collapsed"  HorizontalAlignment="Left" Height="10" Margin="546,367,0,0" VerticalAlignment="Top" Width="100"/>
    </Grid>


</Window>
